cat > .env.example << 'EOF'
# Flask
FLASK_ENV=development
FLASK_SECRET_KEY=changeme_use_strong_key_in_prod

# Postgres
DB_USER=sportcenter
DB_PASSWORD=changeme
DB_NAME=sportcenter_db
DB_HOST=db
DB_PORT=5432
EOF

# Copiás el example como base real (este nunca se commitea)
cp .env.example .env